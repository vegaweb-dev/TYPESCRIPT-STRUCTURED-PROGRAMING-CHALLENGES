/*
Create the following typed arrays:
scores: an array of numbers
names: an array of strings
flags: an array of booleans
Then print all three arrays using console.log.
*/

const scores: number[] = [1, 2, 3, 4, 5, 6];
const names: string[] = ["ana", "carlos", "bob"];
const flags: boolean[] = [true, false, true];

module.exports = { scores, names, flags };
